# frozen_string_literal: true

RSpec.describe Release::Please::Non::Manifest do
  it "has a version number" do
    expect(Release::Please::Non::Manifest::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
